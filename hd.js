var compute;

compute = function(s1, s2) {
	//this is what the program returns
	var distance;
//write code here to set the apporriate distance 


//this is where it gets returned
	return distance;
};

module.exports = {
	compute: compute

};
